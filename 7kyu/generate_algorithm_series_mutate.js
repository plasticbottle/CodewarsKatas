//const mutate = (chromosome, p) => chromosome.split('').map(x => {return (Math.random() <= p) ? (!parseInt(x)*1) : parseInt(x)}).join('');
//https://www.codewars.com/kata/567b39b27d0a4606a5000057
//It is going to check every position and by a given probability p it will decide if a mutation will occur.
//A mutation is the change from 0 to 1 or from 1 to 0.
